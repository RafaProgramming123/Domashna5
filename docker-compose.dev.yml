version: "3.9"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: ghcr.io/${GHCR_NAMESPACE:-example}/backend:dev
    environment:
      NODE_ENV: development
      PORT: 3000

